#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n,k;
        cin>>n>>k;
        vector<int> a(n);int min1=INT_MAX,min2=INT_MAX;
        for(int i=0;i<n;i++)
        {
            cin>>a[i];
            if(a[i]>1)
            min1=min(min1,a[i]);
            if(a[i]!=min1)
            min2=
        }
    }
}